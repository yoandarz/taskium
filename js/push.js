import{VAPID_PUBLIC}from'./config.js';import{currentSession}from'./auth.js';import{saveSubscription}from'./cloud.js';
function b64(s){const p='='.repeat((4-s.length%4)%4),b=(s+p).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(b);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))}
export async function enablePush(){if(!('serviceWorker'in navigator)||!('PushManager'in window))throw new Error('Este navegador no admite notificaciones push.');const perm=await Notification.requestPermission();if(perm!=='granted')throw new Error('No se concedió permiso para notificaciones.');const reg=await navigator.serviceWorker.ready;let sub=await reg.pushManager.getSubscription();if(!sub)sub=await reg.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64(VAPID_PUBLIC)});const s=await currentSession();if(!s)throw new Error('Inicia sesión antes de activar alertas.');await saveSubscription(sub,s.user.id);return true}
