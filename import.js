import{makeTask,defaultSettings,today,daysBetween}from'./model.js';import{put}from'./db.js';
export async function importLegacy(data){let n=0;if(!data||typeof data!=='object')throw new Error('JSON no válido.');for(const old of data.tasks||[]){const kind=old.task_type==='daily'?'recurring':'one_time';const t=makeTask(String(old.text||old.title||''),kind,1);if(!t.text)continue;t.createdAt=(old.created_at||today())+'T00:00:00.000Z';if(kind==='recurring'){t.anchorDate=old.created_at||today();const gen=1+daysBetween(t.anchorDate,today());t.completedCount=Math.max(0,gen-Math.max(0,+old.debt||0));}await put(t);n++;}if(data.settings){const s=defaultSettings();s.snoozeMinutes=Math.min(60,Math.max(1,+data.settings.snooze_minutes||10));s.themeName=data.settings.theme_name||s.themeName;if(Array.isArray(data.settings.alarms_by_day))s.alarmsByDay=data.settings.alarms_by_day;await put(s)}return n}
